name: sync_tree_modile_ui
description: mobile application for access to sync_tree crypto token.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_spinkit: ^5.0.0
  clipboard: ^0.1.3
  modal_bottom_sheet: ^2.0.0
  top_snackbar_flutter: ^1.0.0
  sync_tree_mobile_logic:
    path: /Users/danilafominyh/Documents/sync_tree_mobile_logic

  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^1.2.3

flutter:
  uses-material-design: true
